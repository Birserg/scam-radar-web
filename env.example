# Environment Variables for Static Site Deployment

# ===========================================
# LOCAL DEVELOPMENT (.env.local)
# ===========================================
# Copy these to .env.local for local development
# No special environment variables needed for local dev

# ===========================================
# GITHUB PAGES DEPLOYMENT
# ===========================================
# These are automatically set by GitHub Actions
# GITHUB_PAGES=true
# GITHUB_ACTIONS=true
# NODE_ENV=production

# ===========================================
# OTHER STATIC HOSTING
# ===========================================
# For other static hosts (Netlify, Cloudflare Pages, etc.)
# NODE_ENV=production

# ===========================================
# CUSTOM DOMAIN (Optional)
# ===========================================
# If you're using a custom domain instead of GitHub Pages
# Update the canonical URL in pages/[locale]/index.tsx to match your domain
